<%= form_for @company do |f| %>
  <% if @company.errors.any? %>
    <div><%= @company.errors.full_messages %></div>
  <% end %>
  <div><%= f.text_field :name %></div>
  <div><%= f.submit %></div>
<% end %>
