<div>
  <h1 class="font-bold text-4xl">BusinessAsset#index</h1>
  <p>Find me in app/views/business_asset/index.html.erb</p>
</div>

<%= link_to 'New Asset', new_business_asset_path %>

<br>

<div>
  <% @business_assets.each do |i| %>
    <h4> <%= link_to i.name, edit_business_asset_path(i) %> <%= link_to 'Delete', business_asset_path(i), data: { "turbo-method": :delete, turbo_confirm: "Are you sure?" } %> </h4>
  <% end %>
</div>
